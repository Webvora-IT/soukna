name: manger
description: MANGER — Gérez votre boutique SOUKNA
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  cached_network_image: ^3.3.0
  google_fonts: ^6.1.0
  image_picker: ^1.0.0
  intl: ^0.19.0
  flutter_svg: ^2.0.0
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
